__pycache__/
*.pyc
.venv/
.env
.env.*
!.env.example
config.yaml
config.yml
crucible.toml
*.sqlite
*.sqlite-*
checkpoints.sqlite*
findings.sqlite*
.crucible-workspace/
.crucible-*/
*-run.log
.pytest_cache/
.ruff_cache/
.claude/settings.local.json
.DS_Store
*.o
*.a
*.egg-info/
build/
dist/

# UI (ui/ has its own .gitignore too)
node_modules/
ui/dist/

# tests/ was untracked (issue #30). The Recon re-engineering (issue #34) needs
# its acceptance suite committed, so the test *modules* and the hermetic
# generated fixtures under tests/ are tracked again. Local scratch fixtures and
# caches stay ignored.
tests/**/__pycache__/
tests/.pytest_cache/
tests/fixtures/repos/_local/

# Local scan targets checked out under tests/ for manual runs (not fixtures).
tests/ReactNativeSampleApp/
tests/vulnerable_react_app/
