# Ground-truth manifest (specs.md §12). fixture-c: memory-safety bugs.
# C/C++ carries a higher per-language FP budget (§12) — do not compare its
# FP rate to fixture-py's.
fixture: fixture-c
primary_language: c
bugs:
  - id: c-oob-1
    attack_class: memory_oob_read
    file: src/tlv.c
    line_start: 14
    line_end: 20
    summary: length field from the wire used as memcpy size with no upper bound
    expected_severity: high
  - id: c-obo-1
    attack_class: memory_oob_write
    file: src/name.c
    line_start: 10
    line_end: 14
    summary: off-by-one — loop writes index [len] into a len-sized buffer
    expected_severity: medium
