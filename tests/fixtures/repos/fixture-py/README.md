# fixture-py

Golden fixture — injection classes (specs.md §12). Planted bugs are listed in
`bugs.yaml`; every `BUG(<id>)` comment maps to a manifest entry. Recall per
attack class is measured against this manifest.

Not a real application. Handlers are minimal on purpose so a Hunter can build
a small runnable slice.
